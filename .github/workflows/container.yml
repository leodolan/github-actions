name: Containers

on: push

jobs:
    node-docker:
      runs-on: ubuntu-latest
      container: 
        image: node:16-alpine3.11
        #ports: 
        #volumes:
        #options: --cpus 1
      steps:
        - name: Log Node version
          run: |
            node -v
            cat /etc/os-release
